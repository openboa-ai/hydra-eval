# Evaluator smoke results

Each child directory is one reviewed, sanitized proof that the evaluator completed its minimal question-answer loop. These records are not Hydra benchmark results and must not be used to claim that Hydra improves an agent.

Raw Harbor jobs remain under the ignored `jobs/` directory. Only selected task, verifier, trajectory, judge, timing, token, and provenance evidence belongs here.
